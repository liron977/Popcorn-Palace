#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#    username: sa
#    password:
#    driverClassName: org.h2.Driver
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    hibernate:
#      ddl-auto: update  # This will auto-create/update the schema during tests
#    show-sql: true
#  sql:
#    init:
#      mode: always  # If you want to execute data scripts

#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE  # In-memory H2 database
#    driverClassName: org.h2.Driver
#    username: sa
#    password: password
#    platform: h2
#  jpa:
#    database: H2
#    hibernate:
#      ddl-auto: update  # Automatically creates tables based on your entities
#    show-sql: true  # Optional: Show SQL statements for debugging

spring:
  resources:
    add-mappings: false
  datasource:
    url: jdbc:h2:file:./popcorn_palace_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
